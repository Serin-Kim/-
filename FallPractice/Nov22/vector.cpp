#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<int> v;
    int size = v.size();
    cout << "size - 1 = " << size - 1 << endl;
    cout << "v.size() - 1 = " << v.size() - 1 << endl;
}
